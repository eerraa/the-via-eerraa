import styled from 'styled-components';

export const ConfigureStatusMessage = styled.div`
  padding: 40px 20px;
  max-width: 640px;
  text-align: center;
  font-size: 18px;
  line-height: 1.6;
  color: var(--color_label);
  word-break: keep-all;
  white-space: pre-line;
`;
