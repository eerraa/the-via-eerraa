# Cloudflare Pages and Netlify parse these deployment files line by line.
# core.autocrlf must not turn them into CRLF in a Windows worktree.
/public/_redirects text eol=lf
/public/_headers text eol=lf
